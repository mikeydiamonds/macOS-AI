# PostgreSQL connection (common-postgres)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=GENERATE_RANDOM_32

# Admin credentials (optional - setup via UI on first access)
# NC_ADMIN_EMAIL=admin@example.com
# NC_ADMIN_PASSWORD=changeme123

# Optional: Email notifications
# NC_SMTP_SERVER=smtp.example.com
# NC_SMTP_PORT=587
# NC_SMTP_USERNAME=user@example.com
# NC_SMTP_PASSWORD=your-password
# NC_SMTP_FROM=noreply@localhost
# NC_SMTP_SECURE=false
