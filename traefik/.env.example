# =============================================================================
# Traefik Reverse Proxy Configuration
# =============================================================================

# Traefik v3.1 provides routing for all services via .localhost domains
# No environment variables required for basic local setup

# -----------------------------------------------------------------------------
# Optional Configuration
# -----------------------------------------------------------------------------

# Uncomment to enable debug logging (useful for troubleshooting)
# TRAEFIK_LOG_LEVEL=DEBUG

# -----------------------------------------------------------------------------
# Service Information
# -----------------------------------------------------------------------------
# Dashboard URL: http://traefik.localhost
# The dashboard shows all active services and routes
#
# Traefik automatically discovers Docker services with traefik.enable=true labels
# All services are routed via Host() rules (e.g., chat.localhost, n8n.localhost)
