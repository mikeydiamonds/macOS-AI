# =============================================================================
# Common Infrastructure Services Configuration
# PostgreSQL 17 + Redis Alpine
# =============================================================================

# -----------------------------------------------------------------------------
# PostgreSQL Configuration
# -----------------------------------------------------------------------------

# PostgreSQL superuser username (REQUIRED - Auto-generated by setup script)
# This user will have full access to all databases
POSTGRES_USER=postgres

# PostgreSQL superuser password (REQUIRED - Auto-generated by setup script)
# A random 32-character password will be generated during setup
# You can find this password in SECRETS.md after running setup.sh
POSTGRES_PASSWORD=GENERATE_RANDOM_32

# -----------------------------------------------------------------------------
# Database Information
# -----------------------------------------------------------------------------
# The following databases will be automatically created by init-db.sql:
# - firecrawl    : Used by Firecrawl web scraping service
# - n8n          : Used by n8n workflow automation
# - supabase     : Used by Supabase backend platform (or Supabase uses its own Postgres)
#
# Default database: postgres (PostgreSQL default)
# Connection string format: postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@common-postgres:5432/{database_name}

# -----------------------------------------------------------------------------
# Redis Configuration
# -----------------------------------------------------------------------------
# Redis runs with appendonly persistence enabled automatically
# No password configured (local network only, behind Traefik)
# Connection string format: redis://common-redis:6379

# -----------------------------------------------------------------------------
# Service Usage
# -----------------------------------------------------------------------------
# Services using PostgreSQL:
# - Firecrawl: Stores crawl jobs and results
# - n8n: Stores workflows, executions, and credentials
# - Supabase: Backend database (or may use its own Postgres instance)
#
# Services using Redis:
# - Firecrawl: Caches and rate limiting
# - n8n: Queue management for worker processes
